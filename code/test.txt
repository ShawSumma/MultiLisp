(define clock (timer))
(define (bench) (bench-n 0))
(define (countdown n)
    (if (> n 0)
        (countdown (- n 1))))
(define (bench-n x)
    (countdown 1000)
    (if (< (clock) 2)
        (bench-n (+ x 1))
        x))
(print "Score: ")
(println (bench))